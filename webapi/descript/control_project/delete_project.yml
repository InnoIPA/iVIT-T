tags:
  - control_project
summary: Delete project
description: Delete project

parameters:
  - in: path
    name: uuid
    required: true
    schema:
      type: string

responses:
  '200':
    schema:
      type: string
      description: Successful operation
      example: "Delete project:{prj_name}!"
  '400':
    schema:
      type: string
      description: Error message
      example:
        "This {prj_name} folder does not exist!"
